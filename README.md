# Avainpelaaja OS

Ständimyynnin operatiivinen järjestelmä.

## Käynnistys

`cd server && npm install && npm start`

Avaa `http://127.0.0.1:8080`.

## Testit

`cd server && npm run check && npm test`
