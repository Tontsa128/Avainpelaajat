# Avainpelaaja OS server

Node.js 22 API with SQLite persistence, authentication, RBAC, workspace synchronization, booking conflict checks, geocoding and OSM POI support.

Run `npm install` and `npm start` from this directory.
