export function sellerStats(sellers,bookings,sales=[]){return sellers.map(s=>{const bs=bookings.filter(b=>b.sellerId===s.id);const deals=sales.filter(x=>x.sellerId===s.id).reduce((n,x)=>n+Number(x.deals||0),0);const hours=bs.reduce((n,b)=>n+Math.max(0,(new Date(`1970-01-01T${b.end}:00`)-new Date(`1970-01-01T${b.start}:00`))/3600000),0);return {...s,hours,deals,perHour:hours?deals/hours:0};});}
