export function explainSuggestion(item,place={}){const parts=[];if(item.reason)parts.push(item.reason);if(Number.isFinite(place.distanceKm))parts.push(place.distanceKm+' km myyjän lähtöpisteestä');if(Number.isFinite(place.score))parts.push('paikkapisteet '+place.score+'/100');if(place.history?.salesPerHour)parts.push('historiallinen '+place.history.salesPerHour+' kauppaa/tunti');return parts.join(' • ');}
